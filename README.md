# LogisTrack

LogisTrack is a backend-driven logistics management system that simulates real-world parcel intake, priority-based sorting, and zone-wise dispatch using Spring Boot. It provides a simple web interface to add parcels, process them based on business priority rules, and visualize dispatch queues per delivery zone.
